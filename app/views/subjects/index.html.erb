<!--
<h1>Subjects#index</h1>
<p>Find me in app/views/subjects/index.html.erb</p>
-->
<% @page_title = "Subjects" %>

<%= link_to("<< Back to Menu", {:controller => 'access', :action => 'index'}, :class => 'back-link') %>

<div class="subjects index">
    <h2>Subjects</h2>
    <div><%= pluralize(@subjects.size, 'subject') %> found
    </div>
    <br/>
    <br/>
    <%= link_to(image_tag('plus_sign.png', :size => '11*11', :alt => '+')+" Add New Subject>>", {:action => 'new'}, :class => 'action new') %>
    <table class="listing" summary="Subject list">
        <tr class="header">
            <th>&nbsp;</th>
            <th>Subjects</th>
            <th>Visible</th>
            <th>Pages</th>
            <th>Action</th>
        </tr>
        
        <% @subjects.each do |subject| %>
        <tr class="<%= cycle('odd','even') %>">
            <td><%= subject.position %></td>
            <td><%= subject.name %></td>
            <td class="center"><%= status_tag(subject.visible) %></td>
            <td class="center"><%= subject.pages.size %></td>
            <td class="actions">
                <%= link_to("View pages", {:controller => 'pages', :subject_id => subject.id}, :class => 'action show') %>
                <%= link_to("Show", {:action => 'show', :id => subject.id}, :class => 'action show') %>
                <%= link_to("Edit", {:action => 'edit', :id => subject.id}, :class => 'action edit') %>
                <%= link_to("Delete", {:action => 'delete', :id => subject.id}, :class => 'action delete') %>
            </td>
        </tr>
        <% end %>
    </table>
</div>