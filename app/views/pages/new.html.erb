<!--
<h1>pages#new</h1>
<p>Find me in app/views/pages/new.html.erb</p>
-->
<% page_title = "New Page" %>
<%= link_to("<< Back to list", {:action => 'index', :subject_id => @subject.id}, :class => 'back-link') %>
<div class="pages new">
    <h2>Create pages</h2>
    <%= form_for(:page, :url => {:action => 'create', :subject_id => @subject.id }) do |f| %>
    <!-- Partial thinggie -->
    <%= render(:partial => 'partials/form1', :locals => {:f => f}) %>
    <div class="form-buttons">
        <%= submit_tag("Create Page") %>
    </div>
    <% end %>
</div>